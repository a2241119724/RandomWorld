# [项目名称]

<!-- 为此项目自定义本文件。这是 Claude Code 在每个会话开始时读取的主指令集。
     聚焦于规则、决策和约束——而非属于 docs/ai-context/ 的文档。 -->

## 1. 关键规则

- 对于跨越 3 个以上文件或需要并行调查的任务，**使用子代理**。
- **始终先询问**：git 提交、破坏性更改、重大架构决策、删除文件。
- **停止并重新规划规则**：如果方法失败或发现意外的复杂性，停下来重新评估，而不是强行推进。
- **主动使用可用技能**：`/review-work` 进行代码审查，`/update-docs` 在重大更改后更新文档，`/deploy` 进行部署。
- **Context7 优先**：处理外部库时，先通过 Context7 检查当前文档，再依赖训练数据。
- **想看什么就加日志**：排查运行时问题需要观察某个状态时，直接添加诊断日志，不要问询或犹豫。统一通道与级别约定见 §4「日志调试约定」。

## 2. 操作者模型

<!-- 描述你是谁以及开发是如何进行的，以便 Claude 校准其建议。
     可选的，但对独立/AI 代理驱动的工作流很有价值，
     因为传统关于工作量和复杂度的经验法则不适用。
     填写（或删除）下面的示例。

示例：

- **我是谁。** 独立创始人/小团队负责人，具备概念性技术素养——
  我理解架构和权衡，但不逐行阅读或编写生产代码。
  我把 AI 代理视为专家；我的角色是决策者、指导者和审查者。
- **谁写代码。** AI 代理（Claude + 子代理）。构建工作量≈零。
  时间表由审查、QA 和决策开销决定，而非打字速度。
- **以维护负担而非构建工作量来评估。** "小团队代码太多"/"对一个人来说过度工程化"
  几乎从来不是有效的批评。按技术价值选择库和架构。正确的问题是：
  凌晨两点难调试吗？生产环境难观察吗？供应商出问题难替换吗？
  未来的代理能仅从代码理解它吗？
- **面向决策者校准，而非初级工程师。** 以权衡和原因为先；
  明确说明失败模式——不要假设我能通过阅读 diff 发现细微的 bug。
- **务必标记真正的架构复杂性：** 隐藏的耦合、供应商锁定、
  不必要的抽象、不透明的失败模式。这些才是真正花我成本的复杂性。
- **边界：** 在 git 提交、破坏性更改、删除文件以及
  需要 ≥1 天返工的架构决策之前始终先询问。 -->

## 3. 关键架构决策

<!-- 在此记录已确定的决策，避免 Claude 重复讨论。每条包含简要理由。 -->

<!-- 提示：也列出已确定的决策，避免 Claude 重复讨论。
     示例："选择 REST 而非 GraphQL——已确定，不要建议更改。" -->

<!--
示例：
- **认证**：使用带刷新轮换的 JWT 令牌。原因：无状态，水平可扩展。
- **数据库**：所有表启用 RLS 的 PostgreSQL。原因：行级安全消除认证中间件 bug。
- **状态管理**：仅服务端。原因：单一数据源，无同步 bug。
-->

## 4. 工具使用规则

<!-- 将任务映射到项目的正确工具。 -->

| 任务 | 使用工具 |
|------|----------|
| 库文档查询 | Context7 插件（首先检查） |
| 代码审查 | `/review-work` 技能 |
| 第二意见 | 请求"second opinion"（默认 Codex；说"ask Gemini"用 Gemini，"ask both"同时运行两者） |
| 文档更新 | `/update-docs` 技能 |
| 部署 | `/deploy` 技能 |
| 日志 bug 分析 | `log-bug-fixer` 子代理（读日志前先查 `docs/ai-context/bug-fixes.md`） |
| 运行时状态诊断 | 直接添加 `AWorkerTask.LogProvider` 诊断日志（约定见下） |

### 日志调试约定

**想看什么就加什么日志**——排查 bug 需要观察某个运行时状态时，直接添加诊断日志，不要问询或犹豫。

- **统一通道**：`AWorkerTask.LogProvider(msg, LogManager.LogLevelEnum.X)`（`global using LAB2D.Manager` 已就绪）。
- **级别**：Debug/Trace 只进 `game.log`（不刷 Unity Console），Warning/Error 才进 Console。高频细节用 Trace，关键事件用 Debug。
- **前缀**：`[BrainDiag]`/`[StateDiag]`/`[TaskDiag]`/`[SeekDiag]`/`[MapDiag]`/`[MoveDiag]`/`[StuckDiag]`/`[EnemyDiag]`/`[BuildDiag]`，消息带角色名与坐标。
- **只加在事件点**（状态切换/任务成败/寻路结果/建造状态变化），绝不在每帧/逐格循环加；日志参数构造廉价（禁止 ServiceLocator 查询）。
- **避免重复**：同类事件已有日志时优先增强而非新增；历史教训（高频 Trace 曾致卡顿）见 `docs/ai-context/bug-fixes.md`。

## 5. 技术文档

`docs/ai-context/` 中的文档是为 AI 代理而非人类编写的。保持精简——不确定时，**删除**。

- **只记录当前状态。** 用现在时描述系统*现在*如何工作。没有变更日志，
  没有"我们曾经……"，没有迁移说明，没有未来计划（这些在 `progress.md` 中）。
- **上提跨领域的不变规则。** 适用于整个代码库的规则应放在 `CLAUDE.md`
  或 `spec.md` 中，而不是埋在一个功能的笔记里。在适用的最高层级文档中声明一次。
- **将已发布功能合并到 `spec.md`。** 功能文档在开发期间放在 `docs/` 中，
  发布后合并到 `spec.md`（现在时）——之后归档功能文档。
  架构在 `spec.md` 中，永远不在对话记忆中。
- **bug 修复思路存 `bug-fixes.md`。** 每次日志驱动的 bug 修复把思路追加到
  `docs/ai-context/bug-fixes.md`（现象/根因/修复/验证/教训）。开始新排查前先通读，
  命中历史记录直接引用验证，避免重复排查。

重大更改后运行 `/update-docs` 保持文档与代码同步。

## 6. 编码标准

<!-- 只保留非显而易见或项目特定的标准。
     不要记录标准语言约定——Claude 已经知道这些。 -->

<!--
示例：
- TypeScript 文件：kebab-case（如 `rate-limiter.ts`）
- Swift 文件：PascalCase（如 `SettingsView.swift`）
- 错误响应：`{ success: false, error_code: 'CODE', message: '...' }`
- 所有时间逻辑在 PostgreSQL 中，绝不能放在客户端
-->

## 7. 测试

<!-- 记录测试命令及何时运行。 -->

<!--
示例：
```bash
npm test              # 全部测试
npm run test:unit     # 仅单元测试（~1s）
npm run test:e2e      # E2E 测试（~20s，涉及网络）
```

**何时运行哪些测试：**
| 更改内容 | 运行 |
|---|---|
| 共享工具类 | 单元测试 |
| API 端点 | E2E 测试 |
| 数据库模式 | 集成测试 |
| 部署前 | 全部套件 |
-->

## 8. 隐私与安全

<!-- 记录项目的不可协商安全规则。 -->

<!--
示例：
- 音频/媒体绝不存储——内存中处理，立即删除
- 无用户账户——通过设备 UUID 匿名身份
- 绝不记录个人数据或音频内容
- 绝不在错误消息中暴露系统内部信息（堆栈跟踪、数据库模式）
- 所有外部输入进行输入验证
- 所有数据库表启用 RLS
-->

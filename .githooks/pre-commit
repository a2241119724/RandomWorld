#!/bin/sh
set -e

if command -v pwsh >/dev/null 2>&1; then
  PS_CMD="pwsh"
elif command -v powershell.exe >/dev/null 2>&1; then
  PS_CMD="powershell.exe"
elif command -v powershell >/dev/null 2>&1; then
  PS_CMD="powershell"
else
  echo "PowerShell was not found; cannot create encrypted project archive." >&2
  exit 1
fi

# Disabled: no longer auto-generate encrypted project archive on commit.
# "$PS_CMD" -NoProfile -ExecutionPolicy Bypass -File ".gitarchive/New-EncryptedProjectArchive.ps1"
# git add -f ".commit-archive/ProjectSnapshot.zip.aes"

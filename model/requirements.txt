numpy>=1.26
pandas>=2.0
pyyaml>=6.0
# DeepSeek / OpenAI 兼容端点（LLM 打标签，provider=deepseek）
openai>=1.30
# 网页版 DeepSeek 免费打标签（provider=web；另需一次性 `python -m playwright install chromium`）
playwright>=1.62
scikit-learn>=1.3
matplotlib>=3.8
rich>=13.0
# torch 对 Python 3.14 支持可能滞后；若安装失败请改用 Python 3.11/3.12 的 venv
torch>=2.2
# 导出 ONNX 用
onnx>=1.15
onnxruntime>=1.17
